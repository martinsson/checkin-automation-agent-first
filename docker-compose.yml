services:
  web:
    build: .
    image: checkin-web
    command: uvicorn src.web.app:app --host 0.0.0.0 --port 8001
    env_file: .env
    environment:
      DB_PATH: /data/checkin.db
    volumes:
      - data:/data
    ports:
      - "8001:8001"
    restart: unless-stopped

volumes:
  data:
